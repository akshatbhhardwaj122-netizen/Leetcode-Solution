# 90. [Subsets II](<https://leetcode.com/problems/subsets-ii>)

*All prompts are owned by LeetCode. To view the prompt, click the title link above.*

*[Back to top](<../README.md>)*

------

> *First completed : May 15, 2026*
>
> *Last updated : August 22, 2026*

------

> **Related Topics** : **[Array](<by_topic/Array.md>), [Backtracking](<by_topic/Backtracking.md>), [Bit Manipulation](<by_topic/Bit Manipulation.md>)**
>
> **Acceptance Rate** : **61.86 %**

------

## Solutions

- [0090-subsets-ii/solution.java](<../solutions/0090-subsets-ii/solution.java>)
### Java
#### [solution.java](<../solutions/0090-subsets-ii/solution.java>)
```Java
class Solution {

    void fun(List<Integer> input, List<Integer> output, List<List<Integer>> ans) {

        ans.add(new ArrayList<>(output));

        if(input.size() == 0) return;

        for(int i = 0; i < input.size(); i++) {

            List<Integer> ip = new ArrayList<>(input);
            List<Integer> op = new ArrayList<>(output);

            if(i > 0 && ip.get(i).equals(ip.get(i - 1))) continue;

            op.add(ip.get(i));

            ip.subList(0, i + 1).clear();

            fun(ip, op, ans);
        }
    }

    public List<List<Integer>> subsetsWithDup(int[] nums) {

        List<List<Integer>> ans = new ArrayList<>();
        List<Integer> input = new ArrayList<>();
        List<Integer> output = new ArrayList<>();

        for(int a : nums) {
            input.add(a);
        }

        Collections.sort(input);

        fun(input, output, ans);

        return ans;
    }
}

```

